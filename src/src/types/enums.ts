export enum ServicePlatform {
    UPLAY = "uplay",
    PSN = "psn",
    XBL = "xbl"
}

export enum GamePlatform {
    WINDOWS = "windows",
    PLAYSTATION = "playstation",
    XBOX = "xbox"
}

export enum Region {
    AMERICA = "america",
    EUROPE = "europe",
    ASIA = "asia",
    ALL = "all"
}